<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.jdbc.board.user.mapper.UserMapper">
    <select id="selectUser" resultType="com.nhnacademy.jdbc.board.user.domain.User">
        select *
        from BoardUser
        where userId = #{userId}
    </select>

    <select id="selectUsers" resultType="com.nhnacademy.jdbc.board.user.domain.User">
        select *
        from BoardUser
    </select>

    <insert id="insertUser">
        insert into BoardUser(userId, password, type)
        values (#{userId}, #{password}, #{type})
    </insert>

    <update id="updateById">
        update BoardUser set password='${password}'
        where userId = #{userId}
    </update>

    <delete id="deleteById">
        delete from BoardUser where userId=#{userId}
    </delete>
</mapper>
